# Marks the extensions/ directory as a Bazel package.
# *.BUILD.bazel files here are used as build files for external repositories.
exports_files(glob(["*.BUILD.bazel"]) + ["rules_ros2_srv_support.patch"])
